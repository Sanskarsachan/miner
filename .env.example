# CourseHarvester Environment Variables
# Copy this file to .env.local and fill in the values

# CRITICAL: Gemini API key (server-side only, never expose to client)
# Generate at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_actual_gemini_api_key_here

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_MAX_FILE_SIZE=10485760
NEXT_PUBLIC_RATE_LIMIT_PER_HOUR=5

# Deployment Instructions
# For Vercel deployment:
# 1. Go to https://vercel.com/dashboard
# 2. Select your CourseHarvester project
# 3. Navigate to Settings → Environment Variables
# 4. Add GEMINI_API_KEY (server-side only, NOT NEXT_PUBLIC_)
# 5. Add NEXT_PUBLIC_APP_URL pointing to your Vercel domain
# 6. Redeploy the project
#
# NOTE: Never commit .env.local to git - it contains secrets!

